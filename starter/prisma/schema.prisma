datasource db {
  provider = "sqlite"
  url      = env("./notes.db")
}

generator client {
  provider = "prisma-client-js"
}

model Post {
  id        Int     @id @default(autoincrement())
  PROJECT     String

}

